index.html:
Загрузка скрипта со статистикой перенесена в конец файла с атрибутом async, так как весит много и не влияет на работоспособность страницы.

contacts.js:
0-й элемент getElementsByClassName заменен на querySelector по классу.
Вместо того чтобы каждый раз проходиться по массиву div'ов, чтобы найти нужный, можно найти индекс первого видного элемента, поделив то, насколько прокрутился первый элемент вверх (scrollTop) на количество пикселей, на которые делается отступ сверху, относительно родительского элемента(offsetTop). Так же необходимо взять округление к меньшему, чтобы даже если число было видно немного, на хэдере отображалось именно оно.

styles.css:
Добавлена высота .contact равная scroll-padding'у у .contacts. Иначе после 42 элемента происходит смещение индекса.
